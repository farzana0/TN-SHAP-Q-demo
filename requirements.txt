# TN-SHAP-Q demo: exact multilinear-extension Shapley attribution for QNNs.
# Pure-CPU statevector simulation (numpy only); no torch / GPU required.
# Pinned loosely to the versions the figures were generated with.

qiskit>=1.0          # tested with 2.4.1 (Statevector, SparsePauliOp, QAOAAnsatz)
numpy>=1.24          # tested with 2.2.6
scikit-learn>=1.2    # Iris / breast-cancer datasets + feature selection
matplotlib>=3.7      # figure generation

# Headless execution of the notebooks (one notebook per figure):
#   python -m nbconvert --to notebook --execute --inplace notebooks/NN_*.ipynb
nbconvert>=7.0
nbformat>=5.0
ipykernel>=6.0
jupyter

# The math lives in the in-repo `tnshapq` package, which every notebook imports;
# the notebooks do NOT reimplement Owen integration or the exact enumeration reference.
